# KajMama BD (কাজমামা)

বাংলাদেশের কাজের মানুষ হায়ার করার **স্ট্যান্ডঅলোন** প্রিমিয়াম মার্কেটপ্লেস।

BloodLink, Smart craft corner, Mudi POS, PitchLive — **কোনো কিছুর সাথে যুক্ত নয়**। আলাদা রাউট, আলাদা ডেটা, আলাদা অথ, আলাদা UI।

| | |
|---|---|
| **URL** | `/kajmama` → `http://localhost:3000/kajmama` |
| **ডেটা** | `data/kajmama-store.json` (সার্ভার ফাইল) |
| **অথ কুকি** | `kajmama_session`, `kajmama_admin` |
| **ডেমো কাজদাতা** | `01722222222` / `123456` |
| **ডেমো ওয়ার্কার** | `01711111111` / `123456` |
| **অ্যাডমিন পিন** | `1122` at `/kajmama/admin` |

---

## এজেন্টদের জন্য

ইউজার **Kajmama / KajMama BD / কাজমামা / kajmama** চাইলে:

1. এই ফাইল ও `kajmama.project.json` পড়ুন
2. কোড **শুধু** `src/lib/kajmama/` · `src/components/kajmama/` · `src/app/kajmama/` · `src/app/api/kajmama/`
3. অন্য অ্যাপ (`src/lib/auth`, fashion, mudidokan, cricket, donors) **ইমপোর্ট/শেয়ার করবেন না**
4. হোমপেজ বা অন্য সাইটে লিংক যোগ করবেন না, যদি ইউজার না চায়
5. নতুন ফিচার হলে এই ডক আপডেট করুন

---

## হেডার

**হোম · সকল কর্মী · কর্মী লাগবে · ভাষা · লগইন · রেজিস্টার** — ডানদিকে **অ্যাডমিন** ও (লগইন থাকলে) **নোটিফিকেশন বেল**।

অ্যাডমিন: হেডার, ফুটার, লগইন পেজের লিংক, অথবা সরাসরি `/kajmama/admin` (PIN `1122`)।

সব পেজে নিচের ডানদিকে ফিক্সড চ্যাট: ওয়েবসাইট / Facebook / Email / WhatsApp → অ্যাডমিন।

ওয়ার্কার রেজিস্টারে **সতর্কবার্তা** (সাইটের বাইরে লেনদেন নয়, OTP/জামানত প্রতারণা, ফি কর্মী থেকে) — সম্মতি ছাড়া সাবমিট হয় না।

লগইন ইউজার: বেল আইকনে ইন-অ্যাপ নোটিফিকেশন + **পুশ নোটিফিকেশন** চালু করতে পারে।

---

## নিয়ম (পেমেন্ট, প্রিমিয়াম, অ্যাডমিন)

- **সব কাজের পেমেন্ট ওয়েবসাইটে** — সাইটের বাইরে লেনদেন বন্ধ রাখতে
- **সাইট ফি কর্মী থেকে** — কাজদাতা পুরো দাম দেন; অ্যাডমিন `%` ঠিক করে; কর্মী পান `দাম − ফি`
- স্ট্যাটাস: `pending → accepted → in_progress → completed (পেমেন্ট বাকি) → paid`
- **পেমেন্ট না হলে রেটিং খোলে না**, কর্মী **পরের কাজের জন্য available হয় না**
- ওয়ার্কার রেজিস্টারে **ব্যাংক + মোবাইল ব্যাংকিং আবশ্যক**
- অ্যাডমিন নিজের **ব্যাংক / bKash-Nagad** অ্যাকাউন্ট সেট করে — কাজদাতা সেখানে পেমেন্ট করে
- প্যাকেজ নিলে **অটো প্রিমিয়াম**; মেয়াদ শেষে **অটো বেসিক**
- অ্যাডমিন প্যাকেজ এডিট/যোগ, ক্যাটাগরি যোগ, সব কর্মীর ডেটা (প্যাকেজ, পেআউট) দেখে
- **প্রিমিয়াম বিজ্ঞাপন** — অ্যাডমিন যে প্লেসমেন্টে চায় সেখানে সেট করে (`home_hero`, `workers_top`, `all_pages`, …)
- তারিখ দিয়ে **CSV/Excel ও প্রিন্ট** (`/api/kajmama/admin/export`)
- সার্চ: জেলা **ও উপজেলা**

---

## ফাইল মানচিত্র

```
src/app/kajmama/...
src/app/api/kajmama/...
src/components/kajmama/
src/lib/kajmama/
KAJMAMA.md
kajmama.project.json
```

চালু: `npm run dev` → `http://localhost:3000/kajmama`
