# BloodLink deploy config — use root Dockerfile (not Dockerfile.fashion).
# Smart craft Railway service should override dockerfilePath to Dockerfile.fashion in service settings.

[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
