[build]
builder = "DOCKERFILE"
# Smart craft Railway service uses this repo — fashion Dockerfile avoids BloodLink Postgres copy failures.
# BloodLink Railway service: override in service Settings → Build → Dockerfile Path = Dockerfile
dockerfilePath = "Dockerfile.fashion"

[deploy]
restartPolicyType = "ON_FAILURE"
restartPolicyMaxRetries = 5
